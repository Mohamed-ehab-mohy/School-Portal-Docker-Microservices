@model grades_mvc.ViewModels.GradeFormViewModel

@{
    ViewData["Title"] = "Edit Grade";
}

<div class="page-header">
    <h1>Edit Grade</h1>
    <p>Update grade information</p>
</div>

<div class="row justify-content-center">
    <div class="form-container">
        <div class="card">
            <div class="card-body p-4">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <input type="hidden" asp-for="Id" />

                    <input type="hidden" asp-for="StudentId" />

                    <div class="mb-3">
                        <label asp-for="StudentName" class="form-label fw-semibold text-muted small mb-1">Student</label>
                        <div class="form-control-plaintext ps-2 border-bottom border-secondary">
                            @Model.StudentName
                        </div>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="CourseName" class="form-control @(!ViewData.ModelState.IsValid ? (ViewData.ModelState["CourseName"]?.Errors.Count > 0 ? "is-invalid" : "is-valid") : "")" placeholder="Course Name" />
                        <label asp-for="CourseName"></label>
                        <span asp-validation-for="CourseName" class="invalid-feedback"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Score" class="form-control @(!ViewData.ModelState.IsValid ? (ViewData.ModelState["Score"]?.Errors.Count > 0 ? "is-invalid" : "is-valid") : "")" placeholder="Score" />
                        <label asp-for="Score"></label>
                        <span asp-validation-for="Score" class="invalid-feedback"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="GradeDate" type="date" class="form-control @(!ViewData.ModelState.IsValid ? (ViewData.ModelState["GradeDate"]?.Errors.Count > 0 ? "is-invalid" : "is-valid") : "")" placeholder="Grade Date" />
                        <label asp-for="GradeDate"></label>
                        <span asp-validation-for="GradeDate" class="invalid-feedback"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <textarea asp-for="Notes" class="form-control @(!ViewData.ModelState.IsValid ? (ViewData.ModelState["Notes"]?.Errors.Count > 0 ? "is-invalid" : "is-valid") : "")" placeholder="Notes" style="height: 100px;"></textarea>
                        <label asp-for="Notes"></label>
                        <span asp-validation-for="Notes" class="invalid-feedback"></span>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg"></i> Save
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-x-lg"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
